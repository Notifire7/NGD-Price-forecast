# คำสั่งประจำโฟลเดอร์ — NGD Forecast Dashboard (PTTNGD)

โฟลเดอร์นี้ใช้แปลงไฟล์ forecast ราคาก๊าซของ Marketing เป็น dashboard ส่งลูกค้า
ทำรอบละครั้งประมาณกลางเดือน

**มีโฟลเดอร์ฝาแฝดชื่อ `ANGD Forecast` (AMATANGD)** ใช้ไฟล์ Excel ต้นฉบับ *ไฟล์เดียวกัน*
แต่อ่านชีท `Amata-*` คนละรหัสผ่าน คนละ repo คนละลิงก์
**ห้ามเอาข้อมูล 2 ฝั่งมาปนกันเด็ดขาด** ลูกค้าคนละเจ้า
ไฟล์ `.py` ของทั้งสองฝั่งเหมือนกันทุกตัวอักษร — แก้บั๊กฝั่งไหนให้ก๊อปไปทับอีกฝั่งด้วย
ความต่างทั้งหมดอยู่ใน `groups.json` · `profiles.json` · `template.html`

## ถ้าผู้ใช้บอกว่า "รันรอบเดือน" / "ทำ dashboard เดือนนี้" / วางไฟล์ Excel ใหม่มา

รัน `./monthly.sh` (มันจะหยิบ .xlsx ใหม่ล่าสุดใน `input/` เอง) แล้ว**รายงานผลตามนี้**

1. เส้นแบ่งราคาจริง/ประมาณการ ของแต่ละกลุ่ม ว่าออกมาเป็นเดือนไหน
2. **ผลจาก diff — ส่วนนี้สำคัญที่สุด** โดยเฉพาะบรรทัดที่มีเครื่องหมาย `←`
   นั่นคือเดือนที่เคยแจ้งลูกค้าไปแล้วว่าเป็นราคาจริง แต่ตัวเลขเปลี่ยน
   ถ้าเจอ ให้เตือนผู้ใช้ชัด ๆ ว่าต้องแจ้งลูกค้า อย่ารายงานปนกับตัวเลขอื่น
3. รายชื่อไฟล์ที่ได้ใน `out/`

**หยุดถามก่อน build เสมอ** ถ้า diff มีอะไรผิดปกติ อย่ารัน `--yes` เองโดยผู้ใช้ไม่ได้สั่ง

## ถ้าสคริปต์หยุดเพราะผังคอลัมน์ไม่ตรง

`extract.py` จะบอกว่าช่องไหนคาดว่าเป็นอะไรแต่เจออะไร

- **อย่าใช้ `--no-check` เพื่อให้มันผ่าน ๆ ไป** — ความเสี่ยงคือส่งราคาผิดให้ลูกค้า
- ให้เปิดไฟล์ Excel ดูว่า Marketing ย้ายคอลัมน์จริงไหม แล้วรายงานให้ผู้ใช้ตัดสินใจ
- ถ้ายืนยันว่าย้ายจริง ค่อยแก้ไฟล์ **`groups.json`** (ผังคอลัมน์ไม่ได้ฝังในโค้ดแล้ว)

## กฎที่ห้ามละเมิด

- **ห้ามแตะไฟล์ใน `input/`** เป็นต้นฉบับจาก Marketing อ่านอย่างเดียว
- **ห้ามลบอะไรใน `archive/`** เป็นหลักฐานว่าเคยแจ้งลูกค้าไปว่าเท่าไร
- **ห้ามคำนวณราคาเอง** ทุกตัวเลขต้องดึงจากคอลัมน์ในไฟล์ Excel ตรง ๆ
  โดยเฉพาะ Selling price ของ COGEN — สูตรจริงคือ `Cn + Sn + ส่วนต่าง EPP`
  ที่บังเอิญ EPP เป็น 0 อยู่ ห้ามลัดไปบวก Cn+Sn เอง
- **ห้าม build ไฟล์ `internal` ปนกับไฟล์ที่ส่งลูกค้า** ตัวนั้นเห็นครบทุกกลุ่ม
- ถ้าจะแก้ดีไซน์ dashboard แก้ที่ `template.html` แล้ว build ใหม่
  อย่าไปแก้ไฟล์ .html ใน `out/` โดยตรง เพราะข้อมูลข้างในเข้ารหัสไว้

## รหัสผ่าน

อยู่ใน `profiles.json` **ควรเปลี่ยนทุกเดือน** ถ้าผู้ใช้ยังไม่ได้เปลี่ยน ให้เตือน
ถ้าผู้ใช้ขอให้ตั้งรหัสใหม่ ให้สุ่มแบบอ่านออกได้ ยาว 16+ ตัว
แล้วแสดงตารางสรุปว่ารหัสไหนคู่กับกลุ่มไหนให้ก๊อปไปแจ้งลูกค้า

## กฎเรื่อง git (โฟลเดอร์นี้ push ขึ้น GitHub Pages)

- **ห้าม `git add -f` หรือ `git add` ไฟล์ที่ `.gitignore` กันไว้** ไม่ว่าผู้ใช้จะสั่งหรือไม่
  ถ้าผู้ใช้สั่งให้ commit `profiles.json` / `data.json` / `archive/` ให้ทักท้วงก่อนว่ามันคือ
  รหัสผ่านและราคาแบบ plaintext ที่จะอยู่ในประวัติ git ตลอดไป
- **รัน `python3 preflight.py` ก่อน push เสมอ** ถ้าไม่ผ่าน ห้าม push
- เผยแพร่ผ่าน `docs/` เท่านั้น (สร้างด้วย `publish.py`)
  **ห้าม push ไฟล์ใน `out/`** เพราะมี `internal_*.html` ที่เห็นครบทุกกลุ่ม
- หน้าที่ส่งลูกค้าเป็น **ไฟล์เดียว** `docs/index.html` รวมทุกกลุ่ม แยกสิทธิ์ด้วยรหัส
  ลิงก์เดียวแจกได้ทุกกลุ่ม **ห้ามกลับไปแตกเป็นหน้าแยกรายกลุ่มอีก**
  `publish.py` จะปฏิเสธถ้าเจอ profile ที่เห็นมากกว่า 1 กลุ่ม (กัน internal หลุดขึ้นเว็บ)
- repo เป็น public ⇒ `docs/index.html` ของ **ทุกเดือน** ค้างอยู่ในประวัติ git ถาวร
  โหลดย้อนได้ ดังนั้นรหัสต้องยาวพอจริง ๆ (สุ่ม 16+ ตัว) ห้ามใช้รหัสที่มีแพตเทิร์นเดาได้
  ถ้ารหัสเดือนไหนหลุด ข้อมูลเดือนนั้นถือว่าเปิดถาวร เปลี่ยนรหัสทีหลังไม่ช่วยย้อนหลัง
- **ห้ามใส่รหัสผ่านลงในไฟล์ที่ track อยู่** เช่น README, CLAUDE.md, commit message
- ถ้ารหัสหลุดขึ้น remote ไปแล้ว **การลบ commit ไม่พอ** ให้บอกผู้ใช้ตรง ๆ ว่า
  ต้องถือว่ารหัสนั้นตายแล้ว เปลี่ยนใน `profiles.json` แล้ว build ใหม่

## โครงสร้างโฟลเดอร์

```
input/     ไฟล์ Excel จาก Marketing (อ่านอย่างเดียว, ไม่ขึ้น git)
docs/      หน้าที่เผยแพร่จริงบน GitHub Pages — ขึ้น git
out/       ไฟล์ internal ใช้ภายใน — ไม่ขึ้น git
archive/   data.json ของรอบก่อน ๆ — ไม่ขึ้น git
data.json  ข้อมูลรอบปัจจุบัน — ไม่ขึ้น git
```

| สคริปต์ | หน้าที่ |
|---|---|
| `monthly.sh` | รันครบทุกขั้นของรอบเดือน |
| `extract.py` | Excel → data.json + ตรวจผังคอลัมน์ (อ่านผังจาก `groups.json`) |
| `diff.py` | เทียบสองรอบ |
| `build.py` | data.json + profiles.json → dashboard |
| `split.py` | แตกเป็นไฟล์เดี่ยวต่อกลุ่ม (กรณีส่งไฟล์แนบแทนลิงก์) |
| `publish.py` | build ลง `docs/index.html` **หน้าเดียว** โดยลิงก์ลูกค้าไม่เปลี่ยน |
| `preflight.py` | ตรวจว่าไม่มีความลับหลุดก่อน push |

## บริบทที่ควรรู้

- 4 กลุ่มลูกค้า: `ngd_fo` · `cogen_m8` (Cogen ค่าท่อ -8) · `ngd_lpg` · `cogen_p3` (Cogen ค่าท่อ +3)
- Cn เป็นค่าเดียวกันทุก Block ในกลุ่ม FO/LPG
- COGEN รู้ราคาจริงถึงเดือนของไฟล์ (Sn ใช้ Wellhead เดือน N-1 ที่ประกาศแล้ว)
  ส่วน FO/LPG รู้ถึงเดือนก่อนหน้า (ยังรอ FX เฉลี่ยของเดือนปัจจุบัน)
- หน่วยราคา: บาท/ล้านบีทียู ยังไม่รวม VAT
